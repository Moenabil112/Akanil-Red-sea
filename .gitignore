# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# testing
/coverage

# misc
.DS_Store
*.pem
npm-debug.log*
.env*
# The placeholder template is tracked; real .env files remain ignored above.
!.env.example

# typescript
*.tsbuildinfo

# P4-A: generated Prisma client (run `npm run db:generate`)
/lib/generated/

# P4-A: local database backups and dumps (never commit)
/backups/
*.sql.gz
*.dump

# governing archives and source-resolution artwork stay outside the repo
/project-inputs/
*.zip
SOURCE-PNG/
